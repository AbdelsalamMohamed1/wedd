import 'package:flutter/material.dart';

class WeddingCakeDetails extends StatelessWidget {
  const WeddingCakeDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
