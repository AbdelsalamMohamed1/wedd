import 'package:flutter/material.dart';

class AssistantBotTab extends StatelessWidget {
  const AssistantBotTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(color: Colors.purple,);
  }
}
